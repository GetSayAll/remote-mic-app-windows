# Windows 仓库开发规则

## 规则优先级与适用范围

- 用户当前明确要求优先于通用工作流，但不得违反安全、隐私和平台边界；如两者冲突，先说明冲突点再修改。
- 更近目录中的 `AGENTS.override.md` / `AGENTS.md` 覆盖本文件；本文件是仓库级默认规则。
- 专项文档负责详细流程和证据，本文件负责必须遵守的边界、检查项和完成标准。

## 文档路由

开始任务前，按任务类型读取对应文档：

- Bug、异常和回归：先读 [DEBUGGING.md](DEBUGGING.md) 与 [Bugs/README.md](Bugs/README.md)。
- 日志、诊断和脱敏：先读 [LOGGING.md](LOGGING.md)。
- 分支、提交、PR 和 CI：先读 [BRANCH_MANAGEMENT.md](BRANCH_MANAGEMENT.md)。
- 发布、安装、升级和卸载：先读 [RELEASING.md](RELEASING.md)。
- 产品待办或用户可见功能：先读 [TODO.md](TODO.md)。
- Windows 架构和 API 边界：先读 [TECHNICAL.md](TECHNICAL.md)。
- 真实硬件验收：先读对应的 `Testing/Windows*.md`。
- 外部实现、竞品或实验来源：先读 [ATTRIBUTION.md](ATTRIBUTION.md)。

## 开始任务前检查

开始修改前必须：

1. 确认当前目录、仓库根目录和 `git status`。
2. 检查并保留用户已有的未提交修改，不覆盖、不重置无关文件。
3. 搜索 `TODO.md`、现有测试和相关日志事件，确认是否已有实现或已知边界。
4. 明确本次变更属于文档、前端、Rust 核心、Windows 平台层、安装器/发布或真实硬件验收。
5. 写出预期行为、失败路径和验证方式；需要外部参考时先记录来源和适用边界。

## 产品范围

- 正式产品名称为“无线麦 SayAll Windows 版”，简称“无线麦”或“SayAll”。
- 当前支持目标为小米蓝牙遥控器 2 / RC001 和小米蓝牙遥控器 2 Pro / RC003。两种型号均须分别完成 Windows 真机验收后才能宣称通过。
- macOS 代码不进入本仓库；Windows 代码不回填 macOS 仓库。

## 产品命名与 TODO 同步

- 面向用户的文案优先使用“无线麦 SayAll Windows 版”“无线麦”或“SayAll”；
  `Remote Mic` 仅保留给历史兼容标识、代码标识符和既有发布资产。
- 开发或完成用户可见功能时，必须检查 [TODO.md](TODO.md) 是否有对应条目；
  只有实现并完成验证后才能在同一修改中标记完成。

## 架构边界

- UI 使用 Vue 3，应用宿主使用 Tauri 2，系统实现使用 Rust。
- `sayall-core` 不得依赖 Windows API、Tauri 或 WebView。
- BLE、HID、按键注入和音频设备访问只能位于 Windows 平台层。
- 基础语音路径不得依赖管理员权限或虚拟 HID 驱动（2026-09-23 起不再禁止 Frida，见 ADR 0002 修订记录）。
- 需要提权的能力必须由独立、显式启动的 Helper 承载；主程序保持普通用户权限。
- 按设备源头捕获按键的 HID 宿主内报告层方案（在 `WUDFHost.exe` 内改写 / 清空报告，
  可能借助注入框架）**属可选 Helper 的增强轨**，与上一句边界一致，不进入基础路径；
  Helper 未安装、失败或被禁用时基础语音与基础按键必须不受影响。

## 异步资源与生命周期

- 涉及 BLE、HID、WASAPI、Raw Input、SendInput、Tauri IPC 或后台线程时，开始、结束、取消和异常退出路径必须成对存在。
- 断连、睡眠、应用退出、重复调用和超时路径必须幂等；迟到回调不得重新打开已关闭的会话或覆盖更新一代的会话。
- 重试前必须释放上一轮已取得的资源；释放失败必须记录并在后续周期真正重试。
- 异步操作使用明确的 session、attempt 或 generation 标识，防止旧回调污染新状态。
- 任何 DOWN/UP、打开/关闭、订阅/退订、开始/停止操作都必须保持配对。

## 语音键

- 语音键只支持按下开始、释放结束的实时生命周期。
- 不得为语音键增加双击等待或长按阈值。
- 快速按下/释放、连续会话、断连和异常退出必须保持成对清理。
- 语音键可由用户显式配置"按住说话快捷键"（如右 Alt）：按下注入组合键 DOWN、释放注入 UP，必须严格成对；断连、睡眠、中止和退出必须统一释放。快捷键注入只叠加在 ATVV 语音会话之上，不得替代或延迟语音生命周期。

## 第三方应用边界

- 只能使用公开 API、公开协议、全局快捷键和用户可见的辅助功能界面。
- 禁止读取或修改第三方 App 的私有配置、内部数据库、内存结构或私有协议。
- 禁止把第三方进程注入作为稳定语音主路径。
- 上述三条针对**第三方应用进程**；操作系统自身的 HID 宿主进程（承载遥控器设备的
  `WUDFHost.exe`）内的按设备报告层捕获不在此列——它不读改任何第三方 App 的私有数据，
  只处理本产品支持的遥控器设备的报告，且按 ADR 0002 修订记录归入可选 Helper 的增强轨。

## 验证词汇

- `passed`：实际执行并观察通过。
- `failed`：实际执行但不满足预期。
- `deferred`：依赖当前不可用的 Windows 主机、真实硬件或第三方应用。
- Mac 上的构建和单元测试不能表述为 Windows 或 RC001/RC003 真机通过。

## 外部调用成功判据

- 不得仅凭进程退出码、`S_OK`、布尔返回值或“命令执行成功”宣布功能通过。
- 必须用可观察的副作用确认实际行为，例如状态读回、事件日志、新会话可用、设备/端点状态变化或用户可见结果。
- 异步提交、处理中间状态和最终用户可见结果必须分别记录和验证。

## 文件与来源

- 复制或实质改编外部实现时，在 `ATTRIBUTION.md` 记录仓库、提交和模块。
- 实现新功能前，先调研竞品/参考实现（见 ATTRIBUTION.md 来源矩阵）的既有做法再动手，避免自创时序；参考结论记入 `ATTRIBUTION.md` 并在路线图标注边界。
- 不提交证书私钥、真实蓝牙地址、HID 路径、语音数据、个人路径或第三方未授权二进制。
- 需要移除文件时移动到系统废纸篓，不永久删除。
- 详细日志、发布和 Bug 记录规范分别见
  [LOGGING.md](LOGGING.md)、[RELEASING.md](RELEASING.md) 和
  [Bugs/README.md](Bugs/README.md)。

## 运维与自愈（2026-09-05 会话复盘新增）

- **用户侧零介入原则**：连接类故障（强杀残留、链路僵死、睡眠唤醒、固件抽风）必须由应用代码自愈（自动重试 + 自动无线电恢复等公开 API 手段），不得把"重连、重新配对、重启蓝牙、重启电脑"作为常规解法推给用户；只有公开 API 全部失效的场景才允许 UI 提示人工介入（且提示必须说明原因与预期效果）。
- **部署不得强杀正在连接的应用**：强杀会留下未正常关闭的 BLE 会话，是链路僵死的主要诱因（2026-09-05 实证：安装包强杀后重连循环永不恢复，需无线电开关清除）。部署/升级流程应先请求应用正常退出；若必须强杀，预期可能触发一次自动恢复周期（约 60 秒内自愈）。
- **吞键类抑制器必须做边沿配对**：任何"按下沿可能有条件放行（泄漏进 OS）、释放沿无条件吞下"的非对称吞键都会造成粘键，粘住的键会让下游热键（如输入法语音和弦）整体失效且难以归因。规则：DOWN 漏进 OS 则 UP 必放行（Voice_VibeCoding 同款，2026-09-05 已补入 key_suppressor.rs）。修改 LL 钩子/注入逻辑时，必须为"按住中应用退出/重启/边沿跨进程存活"写单元测试。
- **破坏性批量操作必须先验证目标再执行**：对 GitHub Release/资产等外部资源的删除类 API，选择器必须锁定单个目标并打印将删除的清单核对后再执行（2026-09-05 教训：选择器意外匹配 4 个草稿，误删 9 个附件，靠 CI artifact 才恢复）。
- **交付路径按用户原话执行**：用户要本地包就给本地路径，不自行扩展到上传/发布等额外渠道；额外动作即使"顺手"也先确认。
- **本地测试包先于预览发布（2026-09-09 用户明确要求）**：功能完成后默认只构建本地测试包，给出本地路径并完成安装、升级、启动和相关功能验证；验证完成也不自动授权发布。只有用户在看到验证结果后再次明确要求“发布预览版”，才允许创建发布 Tag、GitHub Release 或上传发布资产。普通的“继续”“做完”“合入 main”均不得解释为发布授权。
- **延迟/时序类改动必须持锁实测**（ConsentStore/GATT 日志等 ground truth），不得凭推理改常量；实验结论当天归档 evidence 与 ATTRIBUTION.md。
- **归因线程、钩子线程等后台机制的"偶发迟到"要按必然事件设计**：60ms 有界等待超时不是错误路径而是常态路径，其后果（如边沿泄漏）必须有成对的兜底，而不是假设它不发生。
- **功能点必须自带日志（2026-09-05 复盘新增，用户明确要求）**：每个功能点（状态判定、分支决策、外部调用结果、关键耗时）都要写结构化日志，确保测试出问题后**直接从日志定位**，而不是靠反复尝试/反复加探针归因。判据：一次用户报障 + 一次日志拉取 = 定位到具体环节；做不到就是日志不够。日志不得含设备身份/语音内容/个人路径（与隐私规则一致）。当前载体：SAYALL_GATT_LOG 诊断日志（gatt_log/gatt_note 标记体系），生产常驻日志系统列入路线图。
- **写完功能必须自验证且最小化修改（2026-09-05 复盘新增，用户明确要求）**：交付任何功能前：① 用真机/实验证明它实际生效（而非"代码写了、编译过了、API 返回 S_OK"——2026-09-05 教训：MTA 线程上 TSF ActivateProfile 返回 S_OK 但不生效，靠探针才揭穿）；② 检查逻辑完备（边界、失败路径、超时、重复调用、跨线程/套间）；③ 修改范围最小化（不顺手重构、不引入无关改动）。三项都过才能交付。
- **延迟优化必须在保证成功率的基础上进行（2026-09-05 晚用户明确要求）**：任何延迟/性能优化的前提是不降低成功率；验证必须覆盖**冷/闲置后首用场景**（不是只有热状态连续测试——2026-09-05 教训：和弦间隔 80ms→20ms 的"4/4 验证"全是热态探针连发，上线后每次闲置首按必失败，且此回归被误诊为"微信输入法钩子休眠"引发两天的错误方向调查；用户字段反馈才揭穿）。热态验证通过 ≠ 可以交付；涉及按键注入/时序的改动，至少要等一次真实闲置后的首用确认。

## 执行效率与 Token 控制（2026-09-09 发布复盘新增）

- **优先使用 GitHub 原生完成通知**：触发 CI 的账号应在 GitHub Actions 通知设置中
  启用 Web 或邮件通知，以便工作流成功、失败、取消后主动通知人。该通知不能唤醒
  正在执行的 Codex 本地任务，因此 Codex 仍按下述固定时间点查询，但不得用高频
  `watch` 代替通知。
- **CI 最多查询三次**：2026-09-09 统计最近 19 次成功的 Windows CI，中位数
  19.23 分钟、P90 19.68 分钟、最长 19.97 分钟。默认在触发后约第 18、21、25 分钟
  查询结构化状态，任务提前收到 GitHub 完成通知时可直接做一次最终查询；总查询次数
  不得超过 3 次。只输出当前步骤、结论和状态变化，不回传未变化的完整步骤列表。
- **限制构建日志进入会话**：编译和测试保留完整日志供故障排查，但正常路径只向
  会话返回末尾摘要、失败上下文和最终统计，不重复传输依赖编译列表、既有 warning
  或未变化的 pending 状态。失败后再按具体步骤读取对应日志片段。
- **复用已完成的同 SHA 证据**：同一精确提交已经通过的测试、构建或 CI 不重复
  执行；发布分支只改版本元数据时，运行发布门禁要求的增量检查和一次完整 CI 即可，
  并在报告中引用已有功能 PR 的验证结果。
- **发布构建不得因旧开发进程改换目录**：构建前检查输出文件锁和运行中进程。普通
  场景优先通过应用自身退出路径释放；发布时若确认是仓库 `target/release` 下的旧
  开发版进程锁住产物，先打印并核对其精确 PID 与可执行文件绝对路径，然后允许终止
  该进程。不得因此新开 worktree、改用隔离 target 或触发一次无缓存全量编译；此例外
  不适用于 `%LOCALAPPDATA%` 下的正式安装版或其他进程。
- **一次收集、一次汇报**：PR、main SHA、Tag、Release 和资产摘要尽量通过单次
  结构化查询收集，最终汇总链接、SHA、验证结论和 deferred 边界，避免逐项重复读取
  或在 commentary 与 final 中重复大段结果。

## 代码管理（2026-09-05 新增，参照 macOS BRANCH_MANAGEMENT.md）

- **功能完成即 commit**：一项功能"实现 + 自验证"完成后必须立即提交（可
  暂缓 push 或 PR，但工作不允许停留在未提交状态）；每个独立工作项一个
  commit，只含该工作项内容。
- **main 只经 PR 合入**：main 不直接开发、不直接 push（包括文档）；功能
  分支从最新 `origin/main` 创建，合入后 fetch 确认一致。
- 详见 `BRANCH_MANAGEMENT.md`。

## 问题定位与开发新需求

### Bug 定位思路

- 先结合日志和现象定位问题原因。
- 如果现有日志不足以定位，先补充必要的结构化日志，再重新复现问题。
- 根据新增日志重新判断并锁定根因，不凭猜测直接修改实现。
- 补充回归测试或测试用例，确保同类问题不会再次出现，然后再进行最小范围修复。

### 新需求的 TDD 流程

实现生产功能或新需求时，按以下顺序执行：

1. 明确定义预期行为。
2. 先编写或更新测试。
3. 运行测试并确认：既有测试通过；新增测试因预期原因失败。
4. 只实现使测试通过所需的最少代码。
5. 运行完整的相关测试套件并使其全部通过。
6. 验证功能的实际行为，而不只验证代码编译或测试结果。
7. 如果实际行为不正确，先修正测试或预期，再修正实现。
8. 只有行为和测试都正确后，才进行重构。
9. 对探索、研究、可行性验证或 PoC 工作，上述流程不是强制要求；应先验证思路或技术方案，进入生产实现后再回到上述流程。

## 验证矩阵

根据改动范围运行最小但完整的相关检查：

- 前端或 Vue：`pnpm test`、`pnpm build`。
- Rust 或共享核心：`cargo fmt --all -- --check`、`cargo test --workspace`、`cargo check --workspace`。
- Windows/Tauri 平台层：`cargo check -p sayall-windows-app --features runtime-simulation`，以及相关 runtime simulation 脚本。
- 安装器、升级、降级或卸载：运行对应的 `scripts/test-windows-*.ps1`，并覆盖优雅退出、用户数据保留和失败收尾。
- 提交前优先运行 `scripts/ci-preflight.ps1`；发布前按 `BRANCH_MANAGEMENT.md` 的要求追加完整检查。
- 真实硬件必须分别记录 RC001 和 RC003 结果；冷态首用、连续会话、快速连按、断连和睡眠恢复不能由仿真结果替代。

## 完成定义

功能或 Bug 修复交付前必须同时满足：

- 实现完成，修改范围与任务直接相关，没有无关重构。
- 相关自动化测试通过，并验证失败路径、重复调用、超时和取消行为。
- 按改动范围完成适用的真实 Windows、API、第三方程序或硬件验证；未具备条件的部分明确标记为 `deferred`，不把自动化结果扩大解释为真实验收。
- 日志能够区分成功、失败、取消、超时、重试、恢复和迟到回调，且符合隐私红线。
- `TODO.md`、测试文档、`ATTRIBUTION.md` 或 Bug 记录已按需要同步。
- 交付说明包含验证命令、结果、提交 SHA（如已提交）和未覆盖边界。
